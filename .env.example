# PUBLIC build defaults (shell / Cloudflare Pages build environment).
# The build does not load .env files. Use explicit environment variables.
TELEGRAPH_URL=https://telestorage.pages.dev
TELEGRAPH_PROJECT=prj_1aNS55cTr8BiqBZy8LWFRw
# PRIVATE: never exported into dist/ or read from a browser-accessible config.
# Static mode accepts this credential only in the browser's runtime session form.
# A Pages build/runtime secret with this name is NOT a browser login mechanism.
TELEGRAPH_API_KEY=
